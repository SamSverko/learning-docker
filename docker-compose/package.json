{
	"name": "docker-compose",
	"version": "1.0.0",
	"description": "Test app to learn Docker.",
	"main": "app.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Sam Sverko",
	"license": "ISC",
	"dependencies": {
		"express": "^4.18.2"
	}
}
